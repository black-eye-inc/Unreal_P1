# Unreal_P1
Gph_A01

Essential git commands to run in "Git Bash" before committing:
$> cd Downloads/Unreal_P0/
$> git config --global core.longpaths true
$> git config --global user.name "Benjamin Brown (UoL)"
$> git config --global user.email "27047602@students.lincoln.ac.uk"

Commands used to initiate git LFS:
$> git lfs track filename/**/*
Commit and then use
$> git lfs ls-files
to see what git LFS is actually tracking